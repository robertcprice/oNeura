# Examples

This directory is reserved for small runnable examples that do not fit under
`demos/` or a specific subproject yet.

It is currently a placeholder so future example code lands in a predictable
location instead of the repo root.
